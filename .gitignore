/target
__pycache__/
.idea/
desktop.ini
/dist

generation-record-*.csv
# `puyo-rusto/art/sprites.py` writes this; the theme ships the rip instead
procedural-sprites.png
# `puyo-rusto/art/rip.py check` writes this: every skin's joins, to be looked at
alignment.png
# and the `menu_shot` example writes these wherever it is pointed, which defaults to here
menu-*.png
# the Puyo Puyo Tetris rip `puyo-rusto/art/rip.py` cuts that sheet out of, kept locally
# rather than carried: it is 12 MiB, and the two sheets it yields - the puyos and the chain
# caption's face - are committed
/puyo-rusto/art/PC _ Computer - Puyo Puyo Tetris - Gameplay Elements - Puyo Puyo Elements.png

# `puyo-rusto/art/music.py`'s source is the same kind of thing and is not here either: a
# directory of converted rips and a `loops.json` of their loop points, `~/Downloads/pp/ogg`
# by default. Point the script at wherever it is to re-cut theme/{menu,music}/*.ogg

# and neither is `puyo-rusto/art/sfx.py`'s: the Puyo Puyo Tetris 2 sound effect rip, 33 MiB
# of WAVs in twelve .acb folders, which the script expects to find beside itself
/puyo-rusto/art/Nintendo Switch - Puyo Puyo Tetris 2 - Sound Effects - Sound Effects/

# `puyo-rusto/art/rip_retro.py check` writes this: every retro theme's cells drawn as a board
# that uses all sixteen link masks, to be looked at
/puyo-rusto/art/retro-alignment.png

# `rustle-fighter/art/rip.py check` writes this: the four colours laid out as a board, so a
# cell that does not sit where the others do shows as an uneven gap, to be looked at
/rustle-fighter/art/gems-check.png

# the arcade and PlayStation rips `rustle-fighter/art/` cuts that game out of - the sprite
# sheets, the 23 QSound VGZ logs and the thirteen zips of effect WAVs - on the same footing as
# every other rip here: spriters-resource downloads under their own names, kept locally.
# `~/Downloads/Super Puzzle Fighter Art/` by default; the scripts say so.

# the three retro rips `puyo-rusto/art/rip_retro.py` cuts the retro themes out of, and the roms
# the board geometry was measured against. Kept locally rather than carried, on the same
# footing as the Puyo Puyo Tetris sheet above: they are spriters-resource downloads under their
# own names, and re-running the script means finding them again.
/puyo-rusto/art/retro/

*.log